// Javascript string & string functions

const string = "We strive hard to provide best experience to our valueable customers";

// prints the length of the string.
console.log(string.length);

// extracts a part of the string & returns it.
console.log(string.slice(3, 14));
console.log(string.slice(26));
console.log(string.slice(-9, -3));
console.log(string.slice(-9));

console.log();
console.log(string.substring(3, 14));

// replaces a part of the string with another one. won't change the original string;
console.log(string.replace("best", "worst"));

var fruits = ['apple', 'mango', 'grapes', 'kiwi']

console.log(a.slice(1,3));